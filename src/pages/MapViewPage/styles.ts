import styled from 'styled-components';

export const Container = styled.div`
    position: absolute;
    bottom: 100px;

    width: 250px;
    height: 56px;

    background: var(--white);

    z-index: 100;
`;